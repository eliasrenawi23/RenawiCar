"""
URL configuration for analytics app.
"""
from django.urls import path

app_name = 'analytics'

urlpatterns = [
    # Add your analytics-related endpoints here
    # Example: path('stats/', views.AnalyticsStatsView.as_view(), name='analytics-stats'),
]
